# Local-only properties: enable SQL initialization for development
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

# Optional: use an in-memory DB locally or local datasource overrides
# spring.datasource.url=jdbc:h2:mem:realtor;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.username=sa
# spring.datasource.password=